function App() {
  return (
    <h1>
      Chair aur react | I learned that we can initialize react by eighter create
      react project way or vite and vercel way and usually vite way is
      preferred, because create react is bulky and vite is in bundle
    </h1>
  );
}

export default App;
